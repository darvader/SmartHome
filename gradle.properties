# Project-wide Gradle settings.
# IDE (e.g. Android Studio) users:
# Gradle settings configured through the IDE *will override*
# any settings specified in this file.
# For more details on how to configure your build environment visit
# http://www.gradle.org/docs/current/userguide/build_environment.html

# Specifies the JVM arguments used for the daemon process.
# The setting is particularly useful for tweaking memory settings.
# OPTIMIZED: Increased memory allocation and added performance flags
org.gradle.jvmargs=-Xmx4096m -XX:MaxMetaspaceSize=1024m -XX:+HeapDumpOnOutOfMemoryError -Dfile.encoding=UTF-8 -XX:+UseParallelGC

# PERFORMANCE BOOST: Enable parallel builds for faster compilation
org.gradle.parallel=true

# Enable Gradle caching for faster incremental builds
org.gradle.caching=true

# MAJOR PERFORMANCE IMPROVEMENT: Enable configuration cache
org.gradle.configuration-cache=true

# Enable Gradle daemon for faster subsequent builds
org.gradle.daemon=true

# Enable build cache
org.gradle.configureondemand=true

# AndroidX package structure to make it clearer which packages are bundled with the
# Android operating system, and which are packaged with your app's APK
# https://developer.android.com/topic/libraries/support-library/androidx-rn
android.useAndroidX=true
# Automatically convert third-party libraries to use AndroidX
android.enableJetifier=true
# Kotlin code style for this project: "official" or "obsolete":
kotlin.code.style=official

# Enable non-transitive R classes for faster builds
android.nonTransitiveRClass=true
# Enable resource namespacing
android.nonFinalResIds=true

# KOTLIN PERFORMANCE OPTIMIZATIONS
kotlin.incremental=true
kotlin.incremental.android=true
kotlin.incremental.js=true
kotlin.incremental.multiplatform=true

# ANDROID BUILD OPTIMIZATIONS
android.enableR8.fullMode=true
android.enableD8.desugaring=true
